{{#if user.fullName}}
<h3 class="text-center pb-3">Selamat Datang {{user.fullName}}</h3>
{{/if}}
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-12">
            <h4 class="pt-5">{{data.titlePage}}</h4>
        </div>
    </div>

    <div class="row">
        <div class="col-md-12">
            <p class="pt-5">{{{data.descriptionPage}}}</p>
        </div>
    </div>
</div>