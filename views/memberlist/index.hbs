<div class="row">
    <div class="col-md-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h6 class="card-title">{{{title}}} <a href="/memberlist/add" class="btn btn-primary btn-xs">Add</a></h6>
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Nama</th>
                                <th>Email</th>
                                <th>Status</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each data}}
                            <tr>
                                <th><a href="/membership/edit/{{_id}}">{{fullName}}</a></th>
                                <td>{{email}}</td>
                                <td>{{role}}</td>
                                <td><a href="/membership/delete/{{_id}}" class="text-danger">Delete</td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>