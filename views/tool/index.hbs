<div class="row">
    <div class="col-md-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h6 class="card-title">{{{title}}} <a href="/tool/photo/add" class="btn btn-primary btn-xs">Add</a></h6>
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Title</th>
                                <th>Published</th>
                                <th>Type</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each data}}
                            <tr>
                                <th><a href="/tool/edit/{{_id}}">{{title}}</a></th>
                                <td>{{formatdate createdAt}}</td>
                                <td>{{ category}}</td>
                                <td><a href="/tool/delete/{{_id}}" class="text-danger">Delete</td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>